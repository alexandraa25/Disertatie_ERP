import { RomanianDayPipe } from './romanian-day.pipe';

describe('RomanianDayPipe', () => {
  it('create an instance', () => {
    const pipe = new RomanianDayPipe();
    expect(pipe).toBeTruthy();
  });
});
